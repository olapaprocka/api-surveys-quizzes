package pl.paprocka.apisurveysquizzes.survey;

import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;
import pl.paprocka.apisurveysquizzes.user.User;

import java.util.List;
import java.util.Optional;

//@Repository
public interface SurveyRepository
        //extends JpaRepository<Survey, Long>
{
//
//    Optional<Survey> findAllBySurveyName(String surveyName);
//
//    List<SurveyQuestion> findAllById(String questionId);


   // List<Survey> findByUser(User user);
}
